# 文件圖片無損提取工具

本腳本 (`extract_images.py`) 用於從 PDF 或 Word (DOCX) 檔案中無損提取高解析度內嵌圖片。

## 🖼️ 核心功能

*   **無損提取**：支援批次拖曳 PDF 或 Word (DOCX) 檔案，自動提取最原始內嵌的高畫質圖片。
*   **灰階轉換與打包**：支援將彩色圖片批次轉換為灰階，節省檔案大小，並自動打包為 ZIP 壓縮檔。
*   **介面整合**：完整整合至 Windows 原生對話框與系統的暫存清理流程。
